require "spec_helper"

RSpec.describe Vinkit do
  it "has a version number" do
    expect(Vinkit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
